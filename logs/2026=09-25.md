# 2026-09-25 — Week 2

* **Minutes:** 1 hour 30 mins
* **Worked on:** Connected the `fs` module to the Node HTTP API, implemented persistent `GET /tasks` and `POST /tasks`, and practiced JSON parsing/stringifying and HTTP response status codes.
* **Blocked on:** Task ID generation and remaining Week 2 objectives.

## 3 sentences

1. I connected the `fs` module to the HTTP server so `GET /tasks` can read tasks from `tasks.json` and return them as JSON.
2. I implemented persistent `POST /tasks` by receiving the request body, reading the existing tasks, adding the new task, converting the updated array to JSON, and writing it back to the file.
3. I learned why a `201` response represents a newly created resource and why the server should only confirm success after `fs.writeFile()` completes successfully.

## Today's key takeaway

**The API now persists tasks instead of keeping them only in memory.**

## DSA
Nill
