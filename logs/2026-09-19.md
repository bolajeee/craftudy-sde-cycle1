# 2026-09-19 — Week 1

* **Minutes:** 1 hour 40 mins
* **Worked on:** Bash scripting — positional arguments, variables, conditional statements, directory validation, exit codes, and timestamps. Started building `backup.sh` for the weekly folder-archiving task.
* **Blocked on:** Nothing

## 3 sentences

1. I learned how Bash scripts receive values through positional arguments such as `$1` and how those values can be stored in variables like `SOURCE_DIR`.
2. I practiced using `if` statements with `[ ]` tests, including `-d` for checking directories, `!` for negation, and `exit 1` to indicate that a command failed.
3. I started building the backup script and learned how command substitution with `$()` can capture the output of commands such as `date` to create a timestamp for the backup filename.
4. I made a clone of the craftstudy project in the linux environment allowing me to work from ubuntu
