# 2026-09-21 — Week 2

* **Minutes:** ~1 hour
* **Worked on:** Setting up the Node.js environment in Ubuntu/WSL and creating the basic Node project structure.
* **Blocked on:** Node.js was not available in Ubuntu initially. WSL was using the Windows `npm` through its PATH, so I installed and configured Node.js with NVM inside Ubuntu.

## 3 sentences
1. I learned that Node.js allows JavaScript to run outside the browser and that `node server.js` tells the Node runtime to execute a JavaScript file.
2. I learned that npm can run commands defined inside `package.json`, so `npm start` executes the `"start": "node src/server.js"` script.
3. I also learned how WSL's PATH can expose Windows programs to Ubuntu and how NVM provides a separate Node.js installation inside the Linux environment.

## DSA
Nill
